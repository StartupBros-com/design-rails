# Provenance

First-party. Built 2026-08-05..08 inside a private engineering harness and
extracted here as a curated snapshot (no history import, by design). The
derivation method — OKLab farthest-point clustering, usage-weighted,
snap-to-most-used-member, status-before-brand, declared-beats-residual,
variant/component-word guards — is first-party; the three adapted IDEAS are
credited in `NOTICE`. Output targets Google's DESIGN.md spec; none of its code
is bundled.

Validation evidence and its dates live in `docs/validation.md`. Claims decay:
the upstream spec is `alpha` and the pinned `@google/design.md@0.4.0` behaviors
in `docs/upstream-limits.md` were live-verified 2026-08-07.
